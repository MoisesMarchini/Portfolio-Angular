import { ImgParallaxDirective } from './img-parallax.directive';

describe('ImgParallaxDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgParallaxDirective();
    expect(directive).toBeTruthy();
  });
});
